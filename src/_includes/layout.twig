<!DOCTYPE html>
<html lang="en">

	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		{% include 'scripts.twig' %}
		<title>{{ title }}
			| TEA Stack</title>
	</head>

	<body class="{{ env.devMode ? 'debug-screens' }} bg-yellow-100 grid grid-cols-4">
		{% include 'sideNav.twig' %}
		{% block body %}
			{{ content|raw }}
		{% endblock %}
	</body>

</html>
